# MED-US pinned dependencies
# Target: Python 3.11 (Windows), NVIDIA GTX 1650 (Turing, sm_75), CUDA 12.1 wheels.
#
# PyTorch MUST be installed from the CUDA 12.1 wheel index, otherwise pip resolves
# the CPU-only build and every SEC evaluation silently runs on CPU:
#
#   pip install -r requirements-torch.txt --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements.txt
#
# See docs/environment.md for the full reproduction recipe.

# --- scientific core -------------------------------------------------------
numpy==2.1.3
scipy==1.14.1
pandas==2.2.3
scikit-learn==1.5.2          # logistic-regression / SVC based MIA attack model (f3)

# --- config, logging, IO ---------------------------------------------------
PyYAML==6.0.2
tqdm==4.67.1

# --- plotting (Pareto fronts, dissertation figures) ------------------------
matplotlib==3.9.2

# --- testing ---------------------------------------------------------------
pytest==8.3.4

# --- evolutionary reference implementation ---------------------------------
# Used in Phase 8 ONLY to validate our own NSGA-II against a trusted baseline.
# The MED-US NSGA-II itself is implemented from scratch (dissertation contribution).
pymoo==0.6.1.3
